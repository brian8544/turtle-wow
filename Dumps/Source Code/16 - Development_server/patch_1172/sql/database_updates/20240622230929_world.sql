-- Assign sounds to Boss Duke Dreadmoore texts.
UPDATE `broadcast_text` SET `chat_type`=1, `sound_id`=60443 WHERE `entry`=30237;
UPDATE `broadcast_text` SET `chat_type`=1, `sound_id`=60444 WHERE `entry`=30238;

